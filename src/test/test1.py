from _operator import add
from turtledemo.chaos import h

from __builtin__ import str


def hel() -> str:
    # <editor-fold desc="this is the test, test for folding comments">
    """

    :rtype: str

    """
    list_one = {'one', '2', 3, {4, 'five'}}
    list_two = {'one', 'two', 'three'}
    list_one.add('test')
    list_two.add('four')
    list_one.append('xxx')


# </editor-fold>

hel()
print(hel())
hel()
hel()
list = {'aaa', 'bbb', 'ccc'}
list.add('add')

